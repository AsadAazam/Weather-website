<!DOCTYPE html>
<html>
    <head>
       <title>{{title}}</title>
       <link rel="icon" href="/img/weather.png">
    </head>
    <body>
        <div class="main_content">
        <h1>{{>header}}</h1>
        <p>{{message}}</p>
        </div>
        <p>{{>footer}}</p>
    </body>
</html>